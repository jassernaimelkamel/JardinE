{% extends 'base.html.twig' %}
{% block body %}

    {% for Repas in Repas %}

        <section>
            <div class="container">
                <div class="panel panel-primary">
                    <div class="panel-heading">
                        <h1 class="panel-title">{{Repas.nomMenu}}</h1>
                    </div>
                    <div class="panel-body">
                <div class="row" style=" width: 3750px;">
                        <div class="col-md-6 col-lg-3 ftco-animate">
                            <div class="pricing-entry bg-light pb-4 text-center">

                                <div><img src="{{ asset('uploads/Repas/' ~ Repas.photo) }}" style="width:200px;height:200px;"></div>
                                <div class="px-4">
                                    <p>{{Repas.type}}</p>
                                </div>
                                <p class="button text-center"><a href="{{path('Ajouter_Abonnements')}}">S'ABONNER</a></p>
                            </div>
                        </div>
                </div>
            </div>
                </div>
            </div>
        </section>
    {% endfor %}
{% endblock %}